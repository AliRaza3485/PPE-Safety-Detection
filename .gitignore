# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# ── ML artifacts ──────────────────────────────────────────
runs/
wandb/
*.pt
*.onnx
*.engine
# Other .pt checkpoints stay out of git. The final deployable model IS committed
# (CI bakes it into the Docker image; it is not DVC-tracked).
models/*.pt
!models/best.pt

# ── DVC-tracked data (data lives in remote storage) ───────
data/raw/
data/*.zip
!data/raw.dvc
# ── Node / Next.js ────────────────────────────────────────
node_modules/
.next/
frontend/out/
frontend/.env*.local

# ── OS / Editor ───────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
